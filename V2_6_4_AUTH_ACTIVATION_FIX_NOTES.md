# DataShield BY 2.6.4 Auth Activation Fix

Исправления:
- отдельный видимый статус авторизации `auth_status`;
- логин теперь всегда запускается и через кнопку, и через Enter;
- добавлен busy-state для кнопки входа;
- дефолтный пользователь: `admin`;
- скрыт технический JSON при успешном входе;
- при ошибке входа технический вывод показывается явно.

Что проверить:
1. открыть UI;
2. логин `admin`;
3. пароль `Admin12345`;
4. нажать кнопку `Войти в консоль`;
5. проверить, что `auth_status` меняется на успешный вход и session block показывает авторизованного пользователя.
